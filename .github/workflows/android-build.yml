name: Build Android APK (PySide6)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-apk:
    runs-on: ubuntu-latest

    env:
      QT_VERSION: "6.8.3"
      WHEELS_DIR: "./wheels"
      P4A_BUILD_PYTHON_VERSION: "3.11.13"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libltdl-dev libtool autoconf automake pkg-config

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PySide6==${{ env.QT_VERSION }} buildozer cython jinja2

      - name: Setup Android SDK paths
        run: |
          export ANDROID_SDK_ROOT="/usr/local/lib/android/sdk"
          export ANDROID_NDK_HOME=$(ls -d /usr/local/lib/android/sdk/ndk/* | head -1)

          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$ANDROID_NDK_HOME" >> $GITHUB_ENV

          mkdir -p "$ANDROID_SDK_ROOT/tools/bin"
          if [ -f "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" ]; then
            ln -sf "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" "$ANDROID_SDK_ROOT/tools/bin/sdkmanager"
          fi

          yes | "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --licenses > /dev/null 2>&1 || true
          "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" "platform-tools" "build-tools;34.0.0" "platforms;android-34" > /dev/null 2>&1 || true

      - name: Download Android wheels from Qt mirror
        run: |
          mkdir -p ${{ env.WHEELS_DIR }}

          curl -L -o ${{ env.WHEELS_DIR }}/PySide6-6.8.3-6.8.3-cp311-cp311-android_aarch64.whl \
            "https://mirror.yandex.ru/mirrors/qt.io/official_releases/QtForPython/pyside6/PySide6-6.8.3-6.8.3-cp311-cp311-android_aarch64.whl"

          curl -L -o ${{ env.WHEELS_DIR }}/shiboken6-6.8.3-6.8.3-cp311-cp311-android_aarch64.whl \
            "https://mirror.yandex.ru/mirrors/qt.io/official_releases/QtForPython/shiboken6/shiboken6-6.8.3-6.8.3-cp311-cp311-android_aarch64.whl"

          ls -lh ${{ env.WHEELS_DIR }}/

      - name: Generate buildozer spec (dry run)
        run: |
          PYSIDE_WHEEL=$(find ${{ env.WHEELS_DIR }} -name "PySide6*.whl" | head -1)
          SHIBOKEN_WHEEL=$(find ${{ env.WHEELS_DIR }} -name "shiboken6*.whl" | head -1)

          echo "Generating buildozer.spec..."
          pyside6-android-deploy \
            -c pysidedeploy.spec \
            --dry-run \
            -f \
            --wheel-pyside="$PYSIDE_WHEEL" \
            --wheel-shiboken="$SHIBOKEN_WHEEL" \
            --name=BrickEmu

      - name: Force Python 3.11 in buildozer.spec
        run: |
          if [ ! -f "buildozer.spec" ]; then
            echo "ERROR: buildozer.spec was not generated!"
            exit 1
          fi

          if grep -q "^python.version" buildozer.spec; then
            sed -i 's/^python.version=.*/python.version = 3.11.13/' buildozer.spec
          else
            echo "python.version = 3.11.13" >> buildozer.spec
          fi

          if grep -q "^requirements" buildozer.spec; then
            sed -i 's/^requirements=.*/requirements = python3,shiboken6,PySide6/' buildozer.spec
          else
            echo "requirements = python3,shiboken6,PySide6" >> buildozer.spec
          fi

          sed -i '/^p4a.commit/d' buildozer.spec

          echo "=== Final buildozer.spec settings ==="
          grep -E "^(python.version|requirements)" buildozer.spec

      - name: Build APK
        run: |
          echo "Starting build with P4A_BUILD_PYTHON_VERSION=$P4A_BUILD_PYTHON_VERSION"
          python -m buildozer android debug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: brick-emu-apk
          path: |
            *.apk
            *.aab
